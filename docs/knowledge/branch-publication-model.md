# Branch Publication Model

Arbor separates authoring from publication.

The repository source branch is the only canonical authoring environment.
Publication branches are generated outputs. They present built project state to
users and should not become places where humans edit source, docs, wiki content,
or release metadata.

Generated publication outputs may include root-level user-facing files such as
`README.md`, `CONTRIBUTING.md`, `LICENSE`, `CHANGELOG.md`, package artifacts,
and Pages-ready site output. On `source`, those files either live under their
canonical source owners or are generated only into `.generated-preview/` or
other build output.

## Branch Roles

### `source`

`source` is the canonical authoring branch.

All implementation, documentation source, wiki source, localization source,
generation tooling, schemas, and validation tooling live here.

Human-authored pull requests target `source`.

### `preview`

`preview` is the generated preview of the next release.

It exists so maintainers can inspect upcoming generated output before promoting
it to `release`.

Its history is disposable and may be regenerated by automation.

Human-authored pull requests should not target `preview`.

### `release`

`release` is the public stable publication branch.

It should be the repository's default public-facing branch once the publication
pipeline exists.

Its contents are generated from `source` and should never be treated as the
canonical source of truth.

Human-authored pull requests should not target `release`.

## Ownership

| Branch | Owner | Human editable |
| --- | --- | --- |
| `source` | Maintainers and approved contributors | Yes |
| `preview` | Automation | No |
| `release` | Automation | No |

Automation publishes. Humans author.

## Contribution Flow

1. Open an issue for discussion, bug reports, RFCs, translation requests, or
   feature proposals.
2. Reach agreement on the intended change.
3. Implement the change on `source`.
4. Submit a pull request targeting `source`.
5. After review and merge, automation generates `preview`.
6. After validation, automation promotes generated output to `release`.

## Generated Content

Generated branches are build outputs.

If generated content needs to change, edit the corresponding source on
`source`. Direct edits to generated branches are invalid and may be overwritten.

`CHANGELOG.md` is generated from `release-notes/Stable/*.md` and
`release-notes/Pre-release/*.md`. It is a publication view, not a source-branch
owner.

Publication automation may generate `preview`, `release`, or both from a
validated `source` state. Stable publication and pre-release preview publication
should be independently runnable, but they should both consume source-owned
inputs rather than hand-authored publication branch changes.

Generated publication previews use Git archive/export rules as their source
filter. Source-only authoring surfaces such as `docs/`, `tools/`, and
`AGENTS.md` are marked `export-ignore` so they do not appear in preview or
release branch output.

## Migration State

The current repository still contains legacy `main`/`canary` workflow language.
Treat that as migration debt until workflows are updated to the
`source`/`preview`/`release` model.
